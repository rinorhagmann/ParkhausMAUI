<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ParkhausMAUI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:ParkhausMAUI.Views"
    Title="ParkhausMAUI">

    <TabBar>
        <Tab Title="Parkhäuser">
            <ShellContent ContentTemplate="{DataTemplate views:MainPage}" Route="MainPage" />
        </Tab>

        <Tab Title="Aktuell">
            <ShellContent ContentTemplate="{DataTemplate views:ActiveParkingPage}" Route="ActiveParkingPage" />
        </Tab>

        <Tab Title="Verlauf">
            <ShellContent ContentTemplate="{DataTemplate views:HistoryPage}" Route="HistoryPage" />
        </Tab>
    </TabBar>

</Shell>